import type { Metadata } from "next";
import "./globals.css";

export const metadata: Metadata = {
  title: "SageByte Assessment",
  description: "Generated by create next app, Static Page with beautiful sidebar desing and custom template for addressing cheeckbox functionality",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className="antialiased">{children}</body>
    </html>
  );
}
